import styled from "styled-components";

const Item = ({ itemId, item }) => {
  // this is one item in the item list
// item to display each item

  return (
    <Wrapper>
      use ItemsDisplay file ...
    </Wrapper>
  );
};

const Wrapper = styled.div`
  border: var(--border);
  height: 15vw;
`;



export default Item;
